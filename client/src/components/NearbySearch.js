import React from 'react'

export default function NearbySearch({ nearby }) {
    return (
        <div>
            {/* {nearby && nearby.map((place) => (
                <div>
                    {place}
                </div>
            ))} */}
        </div>
    )
}
